kind: Template
apiVersion: v1
objects:
  - apiVersion: v1
    data:
      MINIO_ACCESS_KEY: "${MINIO_ACCESS_KEY}"
      MINIO_SECRET_KEY: "${MINIO_SECRET_KEY}"
    kind: Secret
    type: Opaque
    metadata:
      labels:
        app: eagle-api
      name: minio-eagle-test
